# Template only — do NOT commit real values. In a real cluster, create this
# via `kubectl create secret generic francurial-secrets --from-env-file=.env`
# or, better, via your secrets manager's k8s integration (External Secrets
# Operator / Vault CSI driver / cloud provider's Secrets Manager sync).
apiVersion: v1
kind: Secret
metadata:
  name: francurial-secrets
  namespace: francurial
type: Opaque
stringData:
  POSTGRES_PASSWORD: "REPLACE_ME"
  API_KEYS: "REPLACE_ME"
  LIVE_URL_SECRET: "REPLACE_ME"
  WEBHOOK_HMAC_SECRET: "REPLACE_ME"
  CREDENTIALS_ENC_KEY: "REPLACE_ME"
  INTERNAL_SHARED_SECRET: "REPLACE_ME"
  OPENROUTER_KEY: "REPLACE_ME"
  PROXY: "REPLACE_ME_OR_LEAVE_EMPTY_IF_USERS_ALWAYS_SUPPLY_THEIR_OWN"
